{% extends 'base.html.twig' %}

{% block title %}EventPlanning{% endblock %}

{% block body %}
    <h1>EventPlanning</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_planning.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ event_planning.title }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ event_planning.start ? event_planning.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ event_planning.end ? event_planning.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_planning_index') }}">back to list</a>

    <a href="{{ path('event_planning_edit', {'id': event_planning.id}) }}">edit</a>

    {{ include('event_planning/_delete_form.html.twig') }}
{% endblock %}
