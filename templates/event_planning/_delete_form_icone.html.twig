<form method="post" action="{{ path('event_planning_delete', {'id': event_planning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_planning.id) }}">
   <button class="btn-circle btn-danger btn-sm" title="supprimer la classe"><i class="fas fa-trash-alt text-white"></i></button>
</form>
