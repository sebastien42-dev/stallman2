{% extends 'base.html.twig' %}

{% block title %}Modifier un état de facture{% endblock %}

{% block body %}
   <div class="container">
      <h2 class="text-center m-3"><span class="font-weight-bold text-white badge badge-{{ color }}">Modifier l'état de facture {{ bill_state.stateName }}</span></h2>

    
   <div class="card bg-gradient-{{ color }} m-3 p-4">
        {{ include('bill_state/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
   </div>
   <a title="retour à la liste" style="text-decoration:none;" href="{{ path('bill_state_index') }}" class=" mt-3 btn-circle btn-secondary"><i class="fas fa-undo text-white"></i></a>
</div>
{% endblock %}
