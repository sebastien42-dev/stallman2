{% extends 'base.html.twig' %}

{% block title %}modifcation{% endblock %}

{% block body %}
<div class="container">
<h2 class="text-center m-3"><span class="badge badge-info font-weight-bold text-white">Modifier la matière {{ matiere.libelleMatiere }}</span></h2>
    <div class="card bg-primary m-3 p-4">
    {{ include('matiere/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
</div>
<div class="row">
    <a href="{{ path('matiere_index') }}" class="btn btn-secondary btn-sm mr-3">Retour à la liste des matières</a>
    {{ include('matiere/_delete_form.html.twig') }}
    </div>
</div>
   
{% endblock %}
