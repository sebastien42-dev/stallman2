{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
<div class="container">
 <h2 class="text-center m-3"><span class="font-weight-bold text-white badge badge-{{ color }}">Modifier {{user.fonction.libelleFonction}} {{ user.nom }}  {{ user.prenom }}</span></h2>
    
    <div class="card bg-gradient-{{ color }} m-3 p-4">
    {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="row">
        <a title="retour à la liste" style="text-decoration:none;" href="{{ path('user_index') }}" class=" mt-3 btn-circle btn-secondary"><i class="fas fa-undo text-white"></i></a>
        {# {{ include('user/_delete_form.html.twig') }} #}
    </div>
</div>
    
{% endblock %}
