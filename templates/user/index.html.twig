{% extends 'base.html.twig' %}

{% block title %}utilisateurs{% endblock %}

{% block body %}
<div class="container">
    <h2 class="text-center m-3"><span class="badge badge-info font-weight-bold text-white">Listes de Utilisateurs</span></h2>
    <a href="{{ path('user_new') }}" class="btn btn-outline-danger mb-3 mt-3">Créer un utilisateur</a>
    <table class="table table-bordered shadow">
        <thead class="thead-dark text-center">
            <tr>
                <th>N° d'utilisateur</th>
                <th>Email</th>
                <th>Rôle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td class="text-center">
                    <a href="{{ path('user_show', {'id': user.id}) }}" class="btn btn-info m-1">Voir la fiche du utilisateur</a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-info m-1">Modifier le utilisateur</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5"><i>Pas d'utilisateur enregistré</i></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   
    </div>
{% endblock %}
