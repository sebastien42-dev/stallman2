{% extends 'base.html.twig' %}

{% block title %}Modifier la classe{% endblock %}

{% block body %}
<div class="container-fluid">
    <h2 class="text-center m-3"><span class="font-weight-bold text-white badge badge-{{ color }}">Modifier la clase</span></h2>

    {{ include('classe/_form.html.twig', {'button_label': 'mettre à jour'}) }}

    <a title="retour à la liste" style="text-decoration:none;" href="{{ path('classe_index') }}" class=" mt-3 btn-circle btn-secondary"><i class="fas fa-undo text-white"></i></a>

</div>
{% endblock %}
