{% extends 'base.html.twig' %}

{% block title %}Edit Message{% endblock %}

{% block body %}
<h1>Edit Message</h1>

    <div class="container">
        <h2 class="text-center m-3"><span class="font-weight-bold text-white badge badge-{{ color }}">Modifier la salle {{salle.libelleSalle}}</span></h2>
            <div class="card bg-gradient-{{ color }} m-3 p-4">
                {{ include('message/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                {{ include('message/_delete_form.html.twig') }}
            </div>
        <a title="retour à la liste" style="text-decoration:none;" href="{{ path('message_index') }}" class=" mt-3 btn-circle btn-secondary"><i class="fas fa-undo text-white"></i></a>
    </div>

    
{% endblock %}
