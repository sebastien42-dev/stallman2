 <div class="row m-4">

           <div class="col-6">
                <div class="card border-primary shadow-lg">
                    <div class="card-header text-uppercase text-left bg-primary text-white font-weight-bold">
                    {# TODO mettre le mois en injection en lettre par le controller #}
                        <span>Heures pour {{'now'|date('m') }} {{'now'|date('Y') }}</span>
                    </div>
                    <div class="card-body">
                        <canvas id="MontantFactureChart" width="400" height="200"></canvas>
                    </div>
                </div>
            </div>


            <div class="col-6">

                <div class="row">
                    <div class="col-12">
                        <div class="card border-primary shadow-lg">
                            <div class="card-header text-uppercase text-left bg-primary text-white font-weight-bold">
                                 <span>Prochain cours</span>
                             </div>
                            <div class="card-body p-1">
                                 <table class="table table-borderless">
                                     <td>le {{'now'|date('d/m/Y') }} </td>
                                     <td>à 10h</td>
                                     <td>Mathématique</td>
                                     <td>Salle B01</td>
                                 </table>
                            </div>
                         </div>
                    </div>
                </div>
            </div>

       </div>

</div>
