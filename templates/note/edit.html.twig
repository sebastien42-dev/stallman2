{% extends 'base.html.twig' %}

{% block title %}Modifier une note{% endblock %}

{% block body %}
<div class="container">

    <h2 class="text-center m-3"><span class="font-weight-bold text-white badge badge-{{ color }}">Modifier</span></h2>
    
    <div class="card bg-gradient-{{ color }} m-3 p-4">
        {{ include('note/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="row">
        <a title="retour à la liste" style="text-decoration:none;" href="{{ path('note_index') }}" class=" mt-3 btn-circle btn-secondary"><i class="fas fa-undo text-white"></i></a>
        {# {{ include('note/_delete_form.html.twig') }} #}
        </div>
    </div>

    
</div>
{% endblock %}
