
{{ form_start(form) }}
<div class="text-center">
    <h3>
        <span class="badge badge-secondary">facture période : {{ bill.createdAt | date("m - Y") }} pour : {{ bill.user.nom }} {{ bill.user.prenom }}</span>
    </h3>
</div>
    {{ form_widget(form) }}
    <br>
    <div class="text-center m-3">
        <button class="btn btn-{{ color }} border border-white text-white font-weight-bold">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
