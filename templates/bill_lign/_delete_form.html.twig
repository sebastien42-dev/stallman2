<form method="post" action="{{ path('bill_lign_delete', {'id': bill_lign.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bill_lign.id) }}">
    <button class="btn">Delete</button>
</form>
